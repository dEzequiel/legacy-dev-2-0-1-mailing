<table class='mcnTextContentContainer' align='left' border='0' cellpadding='0' cellspacing='0' width='600'>
  <tbody>
    <tr>
      <td
        class='mcnTextContent'
        valign='top'
        style='padding-top:9px; padding-right: 18px; padding-bottom: 9px; padding-left: 18px;'
      >

        <h2 class='null'>
          {{ t('tour_name') }}
          <strong>"{{ products[0].name }} {{ products[0].categoryname['label_' + lang] }}"</strong>
        </h2>
        <br />

        <h3 class='null' style='line-height: 20.7999992370605px;'>
          {{ t('booking_id') }}
          <a>{{ booking.idBooking }}</a>
        </h3>
        <em>{{ t('created_on') }} {{ booking.createdOn | date('d/m/Y') }}</em>
      </td>
    </tr>
  </tbody>
</table>

{% extends '../layouts/yto-layout.njk' %}

{% block body %}
<tr>
    <td align="center" valign="top">
        <!-- BEGIN HEADER // -->
        <table border="0" cellpadding="0" cellspacing="0" width="600" id="templateHeader">
            <tr>
                <td valign="top" class="headerContainer">
                <table border="0" cellpadding="0" cellspacing="0" width="100%" class="mcnTextBlock">
                    <tbody class="mcnTextBlockOuter">
                        <tr>
                            <td valign="top" class="mcnTextBlockInner">

                                <table align="left" border="0" cellpadding="0" cellspacing="0" width="600" class="mcnTextContentContainer">
                                <tbody>
                                    <tr>
                                        <td valign="top" class="mcnTextContent" style="padding-top:9px; padding-right: 18px; padding-left: 18px;">
                                            &nbsp;
                                            <h1>{{ t('your_booking', { id: booking.idBooking }) }}</h1>
                                            <br>
                                            <p><strong>{{ t('expediente') }} {{ affiliate.company.name }}:</strong> {{ booking.idBookingExt }}</p>
                                            <p><strong>{{ t('program') }}: </strong> {{ booking.products[0]['title_' + lang] }} </p>
                                            <p><strong>{{ t('travel_date') }} {{ affiliate.company.name }}:</strong> {{ booking.dates.start.day }} {{ booking.dates.start.monthname_en }} {{ booking.dates.start.year }} </p>
                                            {% set totalpax = booking.paxes.length %}
                                            <p><strong>{{ t('paxes') }}: {{ totalpax }} </strong></p>
											<p>{% for pax in booking.paxes %}
													<strong >{{ loop.index }}.</strong>
													<span >
													  <b>{{ t('name_label') }}</b> {{ pax.name }},  
													  <b>{{ t('lastname_label') }}</b> {{ pax.lastname }}
													  {% if pax.passportfile != null and pax.passportfile.url != null and pax.passportfile.url != '' %}
														{{ t('passport_ok') }} <a target="_blank" href="{{ pax.passportfile.url }}"> {{ t('view') }}</a>
													  {% else %}
														{{ t('no_passport') }}
													  {% endif %}
													</span>
													<br />
											{% endfor %}</p>
                                        </td>
                                    </tr>
									<tr>
										<td valign="top" class="mcnTextContent" style="padding-right: 18px; padding-bottom: 9px; padding-left: 18px;">
											<p class="pre-text">
												{{ t('passport_claim_text') }}
											</p>
										</td>
									</tr>
                                </tbody>
                                </table>

                            </td>
                        </tr>
                    </tbody>
                </table>
                </td>
            </tr>
        </table>
        <!-- // END HEADER -->
    </td>
</tr>


<tr>
    <td align="center" valign="top">
        <!-- BEGIN BODY // -->
        <table border="0" cellpadding="0" cellspacing="0" width="100%" id="templateBody">
            <tr>
                <td valign="top" class="bodyContainer">
                <table border="0" cellpadding="0" cellspacing="0" width="100%" class="mcnTextBlock">
					<tbody class="mcnTextBlockOuter">
						<tr>
							<td valign="top" class="mcnTextBlockInner">
							{% set ca = { txt: t('go_to_booking'), url: 'http://yourttoo.com/edit/booking?code=' + booking.idBooking } %}
							
							{% include "../partials/call-to-action.njk" %}

							<!-- END Call to action -->


							<!-- BEGIN message // -->
							{% include "../partials/yto-team-thanks-message.njk" %}
							{% include "../partials/no-reply-email.njk" %}
							<!-- // END message -->

							</td>
						</tr>
					</tbody>
                </table>
                </td>
            </tr>
        </table>
        <!-- // END BODY -->
    </td>
</tr>
{% endblock %}
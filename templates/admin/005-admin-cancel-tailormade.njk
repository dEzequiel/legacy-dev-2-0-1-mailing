{% extends '../layouts/yto-layout.njk' %}
{% from "../partials/text-block.njk" import textBlock %}

{% block body %}
  <tr>
    <td align='center' valign='top'>
      <!-- BEGIN HEADER // -->
      <table id='templateHeader' border='0' cellpadding='0' cellspacing='0' width='100%'>
        <tr>
          <td class='headerContainer' valign='top'>
            {% call textBlock('100%') %}
              &nbsp;
              <h1>
                {{ t('request_cancelled', { code: request.code }) }}
              </h1>
            {% endcall %}
            {% call textBlock('100%') %}
              <p>
                {{ t('request_made_by') }}
                {{ request.affiliate.company.name }}
                <br />
                {{ t('customer') }}
                <strong>{{ request.title }}</strong>
              </p>
            {% endcall %}
          </td>
        </tr>
      </table>
      <!-- // END HEADER -->
    </td>
  </tr>
  <tr>
    <td align='center' valign='top'>
      <!-- BEGIN BODY // -->
      <table id='templateBody' border='0' cellpadding='0' cellspacing='0' width='100%'>
        <tr>
          <td class='bodyContainer' valign='top'>
            <table class='mcnTextBlock' border='0' cellpadding='0' cellspacing='0' width='100%'>
              <tbody class='mcnTextBlockOuter'>
                <tr>
                  <td class='mcnTextBlockInner' valign='top'>
                    <!-- BEGIN Call to action
                    http://www.yourttoo.com/affiliate/booking-detail?idBooking={{ booking.idBooking }}
                     -->
                    {% if not ca %}
                      {% set ca = { txt: t('go_to_request'), url: 'http://www.openmarket.travel/omt-response?code=[querycode]' } %}
                    {% endif %}
                    {% include "../partials/call-to-action.njk" %}

                    <!-- END Call to action -->

                  </td>
                </tr>
              </tbody>
            </table>
          </td>
        </tr>
      </table>
      <!-- // END BODY -->
    </td>
  </tr>
{% endblock %}

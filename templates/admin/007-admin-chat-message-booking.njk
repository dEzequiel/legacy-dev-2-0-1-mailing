{% extends '../layouts/yto-layout.njk' %}
{% from "../partials/text-block.njk" import textBlock %}

{% block body %}
  <tr>
    <td align='center' valign='top'>
      <!-- BEGIN HEADER // -->
      <table id='templateHeader' border='0' cellpadding='0' cellspacing='0' width='100%'>
        <tr>
          <td class='headerContainer' valign='top'>
            {% call textBlock('100%') %}
              <h1>
                {{ t('new_message_for_booking', { id: booking.idBooking }) }}
              </h1>
            {% endcall %}
          </td>
        </tr>
        <tr>
          <td class='headerContainer' valign='top'>{% include "../partials/admin-affiliate-booking.njk" %}</td>
        </tr>
        <tr>
          <td class='mcnTextContent' valign='top' style='padding: 9px 18px; '>
            <p><strong>{{ t('email') }}</strong> {{ affiliate.contact.bookingContact.email }}</p>
          </td>
        </tr>
      </table>
      <!-- // END HEADER -->
    </td>
  </tr>
  <tr>
    <td align='center' valign='top'>
      <!-- BEGIN BODY // -->
      <table id='templateBody' border='0' cellpadding='0' cellspacing='0' width='100%'>
        <tr>
          <td class='bodyContainer' valign='top'>
            {% include "../partials/chat-message.njk" %}

            <table class='mcnTextBlock' border='0' cellpadding='0' cellspacing='0' width='100%'>
              <tbody class='mcnTextBlockOuter'>
                <tr>
                  <td class='mcnTextBlockInner' valign='top'>
                    <br />
                    <!-- BEGIN Call to action -->
                    {% if not ca %}
                      {% set ca = { txt: t('go_to_booking'), url: url + '#tabdetails' } %}
                    {% endif %}
                    {% include "../partials/call-to-action.njk" %}
                    <!-- END Call to action -->
                    <br />
                  </td>
                </tr>
              </tbody>
            </table>
          </td>
        </tr>
      </table>
      <!-- // END BODY -->
    </td>
  </tr>
{% endblock %}
